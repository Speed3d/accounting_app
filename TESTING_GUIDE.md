# 🧪 دليل اختبار الإصلاحات

## المرحلة 1: اختبار الإصلاحات الأساسية

### ✅ الإصلاح 1: شاشة إنشاء المدير بعد إعادة التثبيت

**الخطوات:**
1. احذف التطبيق من الجهاز بالكامل
2. أعد تثبيت التطبيق
3. افتح التطبيق

**النتيجة المتوقعة:**
- ✓ يجب أن تظهر شاشة "إنشاء مدير" (CreateAdminScreen)
- ✗ يجب ألا تظهر شاشة تسجيل الدخول (LoginScreen)

**الآلية:**
- `database_helper.dart` يكتشف قاعدة البيانات التالفة تلقائياً
- يحذفها ويُنشئ قاعدة جديدة نظيفة
- `getUserCount()` يرجع 0 → يوجه للشاشة الصحيحة

---

### ✅ الإصلاح 2: النسخ الاحتياطي والاستعادة (بدون صور)

**السيناريو 1: الاستعادة على نفس الجهاز**

1. أنشئ حساب مدير جديد
2. أضف بعض البيانات (مستخدمين، منتجات، عملاء)
3. اذهب لـ الإعدادات → النسخ الاحتياطي
4. أنشئ نسخة احتياطية بكلمة مرور: `test123`
5. احفظ مكان الملف (Download/backup-YYYY-MM-DD-HH-MM.accbak)
6. احذف بعض البيانات من التطبيق
7. اذهب لـ الإعدادات → الاستعادة
8. اختر الملف → أدخل كلمة المرور `test123`
9. اختر خيار الدمج

**النتيجة المتوقعة:**
- ✓ تتم الاستعادة بنجاح
- ✓ البيانات المحذوفة ترجع
- ✓ لا توجد رسائل "كلمة مرور خاطئة"

---

**السيناريو 2: الاستعادة بعد إعادة التثبيت**

1. من السيناريو السابق، احتفظ بملف النسخة الاحتياطية
2. احذف التطبيق من الجهاز
3. أعد تثبيت التطبيق
4. أنشئ حساب مدير جديد (يمكن اسم مختلف)
5. اذهب لـ الإعدادات → الاستعادة
6. اختر ملف النسخة القديمة → أدخل كلمة المرور
7. اختر خيار "دمج المستخدمين" (merge)

**النتيجة المتوقعة:**
- ✓ تتم الاستعادة بنجاح
- ✓ البيانات القديمة موجودة
- ✓ المستخدم الجديد + القديم موجودان معاً
- ✗ الصور **مفقودة** (هذا متوقع - سنصلحه في المرحلة 2)

---

**السيناريو 3: اختبار كلمة مرور مع مسافات**

1. أنشئ نسخة احتياطية بكلمة مرور: `  test 123  ` (مسافات قبل وبعد)
2. حاول الاستعادة بكلمة المرور: `test 123` (بدون مسافات)

**النتيجة المتوقعة:**
- ✓ يجب أن تنجح الاستعادة (trim يعمل بشكل صحيح)

---

**السيناريو 4: اختبار النسخ القديمة**

1. إذا كان لديك نسخة قديمة (قبل التحديث):
2. حاول استعادتها

**النتيجة المتوقعة:**
- ✗ رسالة خطأ واضحة: "نسخة احتياطية قديمة"
- ✓ شرح واضح للمشكلة والحل

---

## المشاكل المعروفة (سيتم إصلاحها في المرحلة 2)

### ⚠️ الصور لا تُحفظ في النسخة الاحتياطية

**السبب:**
- النظام الحالي يحفظ قاعدة البيانات فقط
- الصور مخزنة كملفات منفصلة (مسارات في DB)
- عند نقل النسخة لجهاز آخر: البيانات ✓، الصور ✗

**الحل القادم:**
- استخدام `createComprehensiveBackup()` - تحفظ DB + الصور
- كتابة `restoreComprehensiveBackup()` - تستعيد كل شيء

---

## تقرير الاختبار

| الاختبار | النتيجة | ملاحظات |
|---------|---------|---------|
| شاشة إنشاء المدير بعد إعادة التثبيت | ⬜ لم يُختبر | |
| النسخ الاحتياطي | ⬜ لم يُختبر | |
| الاستعادة على نفس الجهاز | ⬜ لم يُختبر | |
| الاستعادة بعد إعادة التثبيت | ⬜ لم يُختبر | |
| كلمة المرور مع مسافات | ⬜ لم يُختبر | |
| النسخ القديمة | ⬜ لم يُختبر | |
| حفظ الصور | ❌ معروف أنه لا يعمل | سيُصلح في المرحلة 2 |

---

## بعد الاختبار

✅ إذا نجحت جميع الاختبارات → انتقل للمرحلة 2 (إضافة نظام الصور)
❌ إذا فشل أي اختبار → أبلغني بالتفاصيل للإصلاح
